[pytest]
; Local to sam_sidecar so pytest doesn't climb to the repo-root pyproject.toml
; and inherit its pythonpath = ["backend", "."], which shadows this dir's
; main.py with backend/main.py (both modules named "main").
