fastapi==0.109.0
uvicorn[standard]==0.27.0
trimesh==4.0.8
plyfile==1.0.2
numpy==1.26.3
pydantic==2.5.3
pyyaml==6.0.1
scipy==1.11.4
laspy[lazrs]==2.5.4
joblib==1.3.2
xgboost==2.1.3
open3d==0.19.0
scikit-learn==1.4.0
optuna>=3.5,<5
# scan_schema — the shared scan-directory schema package (frame / layout /
# fingerprint / validate). Pinned to the published branch so fresh clones and CI
# resolve it without needing the engine-monorepo sibling checkout.
scan-schema @ git+https://github.com/rHedBull/scan_schema.git@main
